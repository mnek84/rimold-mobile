# Copy to .env.production and set your production API URL before building.
# Used by: npm run build:android:production
#
# Must be HTTPS and reachable from client devices (not localhost or LAN IPs).

EXPO_PUBLIC_API_URL=https://api.your-production-host.com/api/v1
